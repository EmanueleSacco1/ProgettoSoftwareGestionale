# This file is intentionally left empty.
#
# It tells Python that the 'backend' directory
# should be treated as a package (a collection of modules).
# This allows us to use imports like `from backend import projects`
# or `from . import persistence` from within the package.