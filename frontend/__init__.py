# This file is intentionally left empty.
#
# It tells Python that the 'frontend' directory
# should be treated as a package (a collection of modules).
# This allows us to use imports like `from frontend import dashboard`
# or `from . import page_base` from within the package.